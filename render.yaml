services:
  - type: web
    name: pedagio-app
    runtime: python
    buildCommand: pip install -r backend/requirements.txt && apt-get install -y poppler-utils || true
    startCommand: cd backend && python main.py
    envVars:
      - key: PORT
        value: 8000
    plan: free
